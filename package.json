{
  "name": "starvault",
  "private": true,
  "version": "0.1.0",
  "description": "AI驱动的全平台收藏与工具管理套件",
  "license": "MIT",
  "author": "TaotaoByte",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=20"
  }
}
